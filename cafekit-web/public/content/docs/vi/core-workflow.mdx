---
title: Workflow chính
description: Luồng CafeKit chính từ intent có boundary tới change đã verify.
---

<DocsHero eyebrow="Main workflow" title="Intent thành code qua contract đã review." description="Khóa scope tại C1, review findings tại C2, implement từng flat task, tạo proof rồi để user đóng tại C3." />

<CoreWorkflowMap />

## Main path

```text
/hapo:brainstorm         # optional khi intent hoặc architecture chưa rõ
  -> /hapo:specs         # C1 -> plan.md + flat tasks -> adversarial review -> C2
  -> dừng planning
  -> /hapo:develop       # invocation mới; từng unblocked task
  -> /hapo:test          # real proof nhỏ nhất nhưng đủ
  -> /hapo:code-review   # correctness, security, regression, contract drift
  -> /hapo:sync          # chỉ observed Status và inline Receipt
  -> C3                  # quyết định completion của user
```

Chat không phải source of truth. New work dùng `specs/<feature>/plan.md` và flat `task-NN-*.md`.

## Contract và execution

`plan.md` ghi C1 scope, exclusions, acceptance criteria, dependencies và C2 decisions. Mỗi task có một outcome, một `Status:`, ownership, dependencies, proof command và final inline `## Receipt`.

Develop scout entrypoints và callers thật trước mutation. Runtime-facing work chưa xong nếu chưa reachable từ route, command, UI mount, worker, service hoặc API boundary thật.

Không âm thầm thay named technology hay runtime choice. Scope change phải quay lại plan và user decision.

## Proof stack

<WorkflowProofStack />

Green command chưa đủ. Exact command liên quan acceptance phải chạy; zero tests và `NO_TESTS` không pass. Review finding chặn completion tới khi fix và reverify.

## State ownership

<WorkflowStateRail />

Chỉ controller update task Status và canonical Receipt. Sync có thể reconcile observed state nhưng không được bịa execution, approval hay completion.

## Alternate routes

| Tình huống | Route |
|---|---|
| Cần câu trả lời có evidence | `/hapo:ask` |
| Cần scoped codebase discovery | `/hapo:scout` |
| Chưa biết root cause | `/hapo:debug` |
| Bug đã diagnose cần sửa | `/hapo:fix` |
| Existing system thiếu docs tin cậy | `/hapo:docs --reconstruct <scope>` |
| Technical decision còn uncertain | `/hapo:research` |
| Bounded numeric optimization rõ ràng | `/hapo:loop` |

Claude Code dùng `/hapo:*`; Codex CLI dùng native `$hapo-*`. OpenCode command chỉ còn trong ghi chú lịch sử 0.16.
