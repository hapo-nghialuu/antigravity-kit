---
title: Cài đặt
description: Cài, nâng cấp và verify CafeKit cho Claude Code hoặc Codex CLI.
---

<DocsHero
  eyebrow="Install"
  title="Bootstrap runtime native vào repo hiện có."
  description="Installer ghi bundle Claude Code hoặc Codex CLI, giữ user-owned settings và lưu version package đã cài."
/>

## Yêu cầu

- Node.js `>=18`
- Project root có quyền ghi
- Claude Code hoặc Codex CLI, hoặc chọn runtime rõ ràng khi cài

## Cài và nâng cấp

```bash
npx @haposoft/cafekit
npx @haposoft/cafekit --upgrade
```

## Runtime được cài

| Runtime | Detection | Installed shape |
|---|---|---|
| Claude Code | `.claude/` | skills, agents, hooks, settings, runtime, `CLAUDE.md` |
| Codex CLI | `.codex/`, `.agents/`, hoặc `--platform codex` | `.agents/skills`, `.codex/agents`, rules, scripts, hooks, runtime và managed `AGENTS.md` block |

Nếu không có marker, installer hỏi runtime cần cài.

## Verify

Claude Code: `/hapo:specs Build a tiny verified feature`

Codex CLI: `$hapo-specs Build a tiny verified feature`

Version được ghi tại `.claude/cafekit.json` hoặc `.codex/cafekit.json`.
