---
title: Cài đặt
description: Bắt đầu với CafeKit Spec trong chưa đầy một phút. Cài đặt chuỗi lệnh workflow spec để phát triển tính năng có cấu trúc.
---

# Cài đặt

Cài đặt CafeKit Spec và bắt đầu sử dụng quy trình phát triển theo spec (spec-driven development) trong dự án của bạn.

## Yêu cầu

Trước khi cài đặt, hãy đảm bảo bạn có:

- **Node.js 18+** - Yêu cầu để chạy CLI
- **Trợ lý lập trình AI** - CafeKit Spec hoạt động với [Claude Code](https://claude.ai/code) hoặc [Antigravity](https://antigravity.google/)
- **Model mạnh mẽ** - Bắt buộc sử dụng **Claude Opus 4.5 (Thinking Mode)** để đảm bảo logic tốt nhất cho các kỹ năng lập kế hoạch (`spec-*`).
- **Git** - Để quản lý phiên bản và cập nhật
- **repomix** - Yêu cầu cho lệnh `/docs init` để gom codebase thành XML cho AI phân tích:
  ```bash
  npm install -g repomix
  # hoặc: pnpm install -g repomix
  # hoặc: yarn global add repomix
  ```

## Khả năng tương thích nền tảng

CafeKit Spec hỗ trợ nhiều nền tảng trợ lý lập trình AI:

| Nền tảng | Định dạng lệnh | Trạng thái |
|----------|---------------|--------|
| **[Claude Code](https://claude.ai/code)** | `/spec-init` | Khả dụng |
| **[Antigravity](https://antigravity.google/)** | `/spec-init` (hoặc `@spec-init`) | Khả dụng |

Quá trình cài đặt sẽ tự động phát hiện nền tảng của bạn và cài đặt định dạng lệnh phù hợp.

## Cài đặt CafeKit Spec

Chạy lệnh cài đặt tại thư mục gốc của dự án:

```bash
npx @haposoft/cafekit-spec
```

Bộ cài đặt sẽ yêu cầu bạn chọn nền tảng:

```
? Select your AI coding assistant platform:
  ❯ Claude Code
    Antigravity
```

Lệnh này sẽ:
- Phát hiện cấu trúc dự án của bạn
- Cài đặt bộ lệnh cốt lõi (`/spec-init`, `/spec-requirements`, `/spec-design`, `/spec-tasks`, `/spec-status`, `/code`, `/test`, `/review`)
- Cấu hình workspace cho quy trình phát triển theo spec

## Những gì được cài đặt

Sau khi chạy lệnh cài đặt, bạn sẽ có framework CafeKit trong dự án:

### Claude Code
```
.claude/
├── commands/
│   ├── spec-init.md
│   ├── spec-requirements.md
│   ├── spec-design.md
│   ├── spec-tasks.md
│   ├── spec-status.md
│   ├── code.md
│   ├── test.md
│   ├── review.md
│   └── docs.md
├── agents/
│   ├── tester.md
│   ├── code-reviewer.md
│   ├── fullstack-developer.md
│   └── debugger.md
├── skills/
│   └── specs/
└── ROUTING.md
```

### Antigravity
```
.agent/
├── workflows/
│   ├── spec-init.md
│   ├── spec-requirements.md
│   ├── spec-design.md
│   ├── spec-tasks.md
│   ├── spec-status.md
│   ├── code.md
│   ├── test.md
│   ├── review.md
│   ├── docs-init.md
│   └── docs-update.md
├── agents/
│   ├── frontend-specialist.md
│   ├── test-engineer.md
│   └── code-archaeologist.md
├── skills/
│   └── specs/
└── rules/
    └── GEMINI.md
```

### Thiết lập Documentation (Chỉ Claude Code)

Sử dụng lệnh `/docs` để tạo documentation cho dự án:

```bash
# Khởi tạo bộ docs nền của dự án
/docs init

# Hoặc cập nhật docs hiện có
/docs update
```

Lệnh này sẽ tạo:
- File context theo nền tảng (`CLAUDE.md` hoặc `.agent/rules/AGENTS.md`)
- Thư mục `docs/` với:
  - `codebase-summary.md`
  - `project-overview-pdr.md`
  - `code-standards.md`
  - `system-architecture.md`
  - `design-guidelines.md`
  - `deployment-guide.md`
  - `project-roadmap.md`

**Lưu ý:** `/docs` được cung cấp dưới dạng `/docs init` và `/docs update`. Mức độ hoạt động phụ thuộc vào khả năng của môi trường AI và context của dự án.

## Xác nhận cài đặt

Mở trợ lý lập trình AI của bạn và gõ lệnh init cho nền tảng tương ứng:

### Claude Code
```
/spec-init
```

### Antigravity
```
/spec-init
# hoặc
@spec-init
```

Bạn sẽ thấy workflow khởi tạo spec bắt đầu.

## Khắc phục sự cố

### Không tìm thấy lệnh

Nếu các lệnh spec không hoạt động:

1. **Kiểm tra vị trí file** - Đảm bảo các file nằm đúng thư mục:
   - Claude Code: `.claude/commands/`
   - Antigravity: `.agent/workflows/`
2. **Khởi động lại trợ lý AI** - Một số công cụ cần khởi động lại để phát hiện lệnh mới
3. **Kiểm tra định dạng file** - Các lệnh phải là file `.md` với frontmatter hợp lệ
4. **Kiểm tra lựa chọn nền tảng** - Chạy lại bộ cài đặt và xác nhận đã chọn đúng nền tảng

### Sai thư mục

Nếu CafeKit cài đặt sai thư mục, bạn có thể di chuyển file thủ công:

```bash
# Cho Claude Code - đảm bảo .claude/commands/ tồn tại
mkdir -p .claude/commands/

# Cho Antigravity - đảm bảo .agent/workflows/ tồn tại
mkdir -p .agent/workflows/

# Chạy lại bộ cài đặt
npx @haposoft/cafekit-spec
```

## Bước tiếp theo

Bây giờ CafeKit Spec đã được cài đặt:

- **[Hướng dẫn nhanh](/docs/getting-started/quickstart)** - Tạo spec đầu tiên trong 5 phút
- **[Quy trình Spec](/docs/guides/spec-workflow)** - Tìm hiểu đầy đủ chuỗi lệnh spec

---

**Cần trợ giúp?** Mở issue trên [GitHub](https://github.com/haposoft/cafekit) hoặc liên hệ hỗ trợ.
