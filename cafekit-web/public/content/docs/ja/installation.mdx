---
title: インストール
description: Claude Code または Codex CLI 向け CafeKit の install、upgrade、verification。
---

<DocsHero
  eyebrow="Install"
  title="既存 repo に native runtime を bootstrap。"
  description="Installer は Claude Code または Codex CLI bundle を書き、user-owned settings と installed package version を保持します。"
/>

## Requirements

- Node.js `>=18`
- 書き込み可能な project root
- Claude Code、Codex CLI、または明示的 runtime selection

## Install and upgrade

```bash
npx @haposoft/cafekit
npx @haposoft/cafekit --upgrade
```

## Installed runtimes

| Runtime | Detection | Installed shape |
|---|---|---|
| Claude Code | `.claude/` | skills、agents、hooks、settings、runtime、`CLAUDE.md` |
| Codex CLI | `.codex/`、`.agents/`、または `--platform codex` | `.agents/skills`、`.codex/agents`、rules、scripts、hooks、runtime、managed `AGENTS.md` block |

Marker がない場合、installer が runtime を質問します。

## Verify

Claude Code: `/hapo:specs Build a tiny verified feature`

Codex CLI: `$hapo-specs Build a tiny verified feature`

Version は `.claude/cafekit.json` または `.codex/cafekit.json` に記録されます。
