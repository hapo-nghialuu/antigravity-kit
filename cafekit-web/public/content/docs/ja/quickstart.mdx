---
title: Quickstart
description: Scope decision から verified implementation まで最初の process-first CafeKit workflow。
---

<DocsHero eyebrow="First run" title="One feature を verified loop に通す。" description="Scope を確認し、plan を review、one task を execute、verify、review、proof sync、completion decision を行います。" />

<Callout type="info">CafeKit が初めてなら [step-by-step tutorial](/docs/tutorial) を利用してください。</Callout>

## 1. Install

```bash
npx @haposoft/cafekit
```

## 2. Spec creation and review

```text
/hapo:specs Build a meeting transcript extension with AI summaries
```

Specs は C1 を開き、次を作成します:

```text
specs/<feature>/
├── plan.md
└── task-01-*.md
```

C2 で findings を accept、revision request、または named limitation として KEEP。Planning は C2 後に停止します。

## 3. Explicit implementation

```text
/hapo:develop <feature>
```

Develop は one unblocked task ずつ実行し、exact planned proof を run、controller が final inline Receipt を書きます。

## 4. Verify and review

```text
/hapo:test --full
/hapo:code-review --pending
```

`NO_TESTS` と zero executed tests は pass ではありません。

## 5. Sync and close

```text
/hapo:sync <feature>
```

Sync は observed state と proof だけを更新します。C3 で user が current receipts と unresolved limitations が close に十分か決定します。

<QualityGate />
