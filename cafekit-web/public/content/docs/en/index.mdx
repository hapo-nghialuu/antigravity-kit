---
title: CafeKit Documentation
description: Claude Code-first documentation for CafeKit's spec-driven runtime, workflows, and reference material.
---

# CafeKit Documentation

CafeKit is a **Claude Code-first runtime bundle** for spec-driven development. It installs the workflows, agents, hooks, and runtime context needed to move from idea to verified implementation without relying on ad-hoc prompts.

## Who This Is For

Use CafeKit if you want:

- a structured feature workflow instead of one-off prompting
- spec artifacts that stay with the project
- task-level execution with explicit verification
- review and docs checkpoints before claiming work is done

## Core Loop

```text
Idea
  -> /hapo:brainstorm (if unclear)
  -> /hapo:specs
  -> /hapo:develop
  -> /hapo:test
  -> /hapo:code-review
```

Common support commands:

- `/hapo:brainstorm <idea>` for scout-first ideation before a spec
- `/hapo:specs --validate <feature>`
- `/hapo:sync audit <feature>`
- `/hapo:generate-graph <request>`
- `/docs init` and `/docs update` for project documentation workflows

## What CafeKit Installs

At install time, CafeKit adds a runtime bundle to `.claude/`:

```text
.claude/
├── skills/
├── agents/
├── hooks/
├── settings.json
├── runtime.json
├── status.cjs
└── CLAUDE.md
```

The primary release surface is:

- `/hapo:brainstorm`
- `/hapo:specs`
- `/hapo:develop`
- `/hapo:test`
- `/hapo:code-review`
- `/hapo:generate-graph`

## Documentation Map

### Getting Started

- [Installation](/docs/getting-started/installation)
- [Quickstart](/docs/getting-started/quickstart)

### Core Concepts

- [Spec Lifecycle](/docs/core-concepts/spec-lifecycle)
- [Task Registry](/docs/core-concepts/task-registry)

### Workflows

- [`/hapo:specs`](/docs/workflows/specs)
- [`/hapo:develop`](/docs/workflows/develop)
- [`/hapo:test + /hapo:code-review`](/docs/workflows/test-review)
- [`/hapo:sync`](/docs/workflows/sync)
- [`/hapo:generate-graph`](/docs/workflows/generate-graph)

### Reference

- [File Structure](/docs/reference/file-structure)
- [Command Cheatsheet](/docs/reference/commands)
- [FAQ](/docs/faq)

## Product Position

- **Claude Code** is the active runtime today.
- **Antigravity** is coming soon.
- **Cursor** is coming soon.

## Next Step

Start with [Installation](/docs/getting-started/installation), then follow the [Quickstart](/docs/getting-started/quickstart).
