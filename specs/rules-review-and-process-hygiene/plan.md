# Rules: review precedence and process hygiene
Specs-Contract: process-first-ready-v1

## Scope decision (C1 — 2026-09-02)

- Existing: CafeKit ships eight rule files from `packages/spec/src/claude/rules/` (`ai-dev-rules`, `hook-protocols`, `manage-docs`, `orchestrator`, `skill-domain-routing`, `skill-workflow-routing`, `state-sync`, `workflow`; 530 lines total). The installer copies the whole directory per platform through `copyRulesDirectory` (`packages/spec/bin/phases/claude-runtime.js:260-277`, gated by the `rules` capability, enabled for both platforms at `bin/lib/context.js:92,119`): the Claude copy passes through `normalizeSourcePaths` (identity unless the text contains a `packages/spec/src/` path — `bin/lib/copy-utils.js:28-39`), the Codex copy through `normalizeCodexBody` (`bin/lib/codex-install.js:288-298`). Codex then overlays two native overrides from `packages/spec/src/codex/rules/` (`hook-protocols.md`, `state-sync.md`; `bin/phases/codex-runtime.js:111-123`, run after the copy at `:156-157`); `copyManagedTree` never deletes, and `migration-manifest.json` has no `rules` key, so a new rule file ships with no installer or manifest edit and no obsolete-cleanup risk. The npm `files` whitelist already includes `src` (`packages/spec/package.json:18-20`). Existing guards: four ordinary static probes over rule files in the harness `checks` array (`packages/spec/scripts/run-skill-self-tests.mjs:4231,:4665,:4679,:4916`) — no rule has a mutation checker; installed Claude/Codex rule parity (`packages/spec/bin/__tests__/codex-native.test.js:3107-3115`); the packed-tarball `REQUIRED_PAYLOAD` list (`packages/spec/bin/__tests__/package-inventory.test.js:110-145`, asserted by `assertCleanInventory` at `:272-275`). No test pins the count or exact set of rule files, and no line/byte budget covers `rules/`. The reference rules `~/Desktop/cafekit-ref/.claude/rules/review-audit-self-decision.md` (34 lines) and `process-management.md` (40 lines) have no CafeKit counterpart: nothing shipped covers background-process hygiene, audit-versus-decision precedence, or ID-free code artifacts.
- Minimum change: add two rule files to `packages/spec/src/claude/rules/` carrying the reference guidance in CafeKit vocabulary — `review-audit-self-decision.md` (verified decisions stand unless the audit brings new evidence or changed context, naming the verification source when rejecting a concern; explicit user decisions, including the C1/C2/C3 dispositions defined in `workflow.md`, are never silently undone — a reversal proposal presents the original decision, the audit concern, the trade-off, and concrete options, then waits for the user; identify what the code actually stores, protects, or exposes before applying a security or robustness finding; scout the repository before asking, deferring ask-back conditions to `hapo:ask`; do not add plan, phase, CP/AC, audit, or finding IDs to new code comments, migration names, test names, or commit messages) and `process-management.md` (track command, PID, port, worktree; check for an owner then reuse or stop it; deterministic port per project or worktree, stopping the stale owner instead of taking another port; stop what you started before its task, session, or wave worktree ends; reconcile periodically; clean stop before hard kill; stop only processes you own and never a broad kill pattern). Add one consult bullet naming both rules to the Codex runtime template, written in Codex-native vocabulary, so Codex has a real read path. Guard the pair with two ordinary static probes (required anchors plus forbidden negation patterns) and one transform-safety probe, then assert installed parity for both runtimes and packed payload.
- Expansion signals: none — one subsystem (shipped rules), six touched files, two sequential tasks.
- User decision: **KEEP** — port exactly the two reference-only rules; do not fold reference deltas into the six thematically paired rules, whose routing and state machinery differ by design.

## Out of scope

- Rewriting the six paired rules toward the reference (`ai-dev-rules`, `manage-docs`, `orchestrator`, `workflow`, the routing pair) or syncing `hook-protocols`/`state-sync` — different machinery, conscious exclusion.
- Codex-native overrides under `packages/spec/src/codex/rules/` for the new rules; the transformed Claude source stays the single home (A3).
- A Claude-side consult pointer in `packages/spec/src/claude/CLAUDE.md:14`: Claude Code auto-loads `.claude/rules/*.md`, so the pointer is only required for Codex.
- Retro-renaming the existing test names and comments that embed plan or finding IDs (`packages/spec/bin/__tests__/validator-grounding.test.js:3131,3160`; `bin/phases/setup-rtk.js:7-8,20`); the shipped rule governs new artifacts only.
- Guide edits: `packages/spec/README.md:91,117` show `rules/` as a directory only, and `README.md` enumerates no rule file.
- `docs/` living documents and changelog — deferred to the docs-sync flow.
- Project-local process conventions (Herdr panes, `.claude/settings.*.json` profiles) — not package payload.
- Any claim that a live model obeys the new rules; proof is source text plus installed placement.
- Refreshing this repository's per-machine installed `.claude/rules/` copy — post-landing operations.

## Coverage profile

| ID | Outcome | Change kinds | Material surfaces | Ambiguity/action | Risk/evidence | Required proof |
|---|---|---|---|---|---|---|
| CP-01 | A shipped rule `review-audit-self-decision.md` states: keep a decision verified by source, tests, or an empirical check unless the audit adds new evidence or the context changed, and name the verification source when rejecting a concern; never silently undo explicit user decisions (the C1/C2/C3 dispositions defined in `workflow.md:15-19,62-63`, thresholds, libraries, schema shape, scope, UX trade-offs) — present the original decision, the audit concern, the trade-off, and concrete options, then wait for the user; identify what the code actually stores, protects, or exposes before applying a security or robustness finding; scout the repository before asking, deferring ask-back conditions to `hapo:ask` rather than restating them; do not add plan, phase, CP/AC, audit, or finding IDs to new code comments, migration names, test names, or commit messages | add | installed instruction surface | none | elevated — Claude auto-loads every rule into every session; Codex reach depends on the template pointer of CP-03 | source |
| CP-02 | A shipped rule `process-management.md` states the orphan failure mode and: track every background process you start (command, PID, port, worktree) and prefer an observable background facility over an unobservable detached run; check for an existing owner before starting and reuse or stop it; bind a deterministic port per project or worktree and, on a busy port, stop the stale owner rather than taking another port; stop what you started before its task, session, or wave worktree ends; reconcile running processes periodically; stop cleanly before escalating to a hard kill; stop only processes you started or clearly own, never matching a broad kill pattern | add | installed instruction surface | none | elevated — same surface; complements wave worktree release (`packages/spec/src/claude/skills/develop/references/parallel-waves.md:22,41,60`) | source |
| CP-03 | Both rules reach both runtimes: `.claude/rules/<file>` is byte-equal to source; `.codex/rules/<file>` equals `normalizeCodexBody(source, sourcePath)` and equals source with only `hapo:` skill names renamed; no `packages/spec/src/codex/rules/<file>` override shadows them; the Codex runtime template carries a Codex-native consult bullet naming both rules, since Codex has no directory auto-load and the template ships without transformation; the packed tarball lists both source files. Limitation: this proves fresh installs — a pre-existing user-created file of the same name is preserved by design (`bin/lib/managed-writer.js:95-109`) | integrate | installer copy path, Codex runtime template, npm pack payload | none | elevated — cross-runtime packaging and instruction reach | installed |
| CP-04 | Static probes pin the required clauses of both rules together with forbidden negation patterns (audit findings overriding a prior decision, proceeding without waiting for the user, taking another port instead of stopping the stale owner, treating orphaned processes as acceptable), and one probe proves the Codex transform changes nothing in either rule except `hapo:` skill names | add | checker (static pass) | none | routine — probe shape at `run-skill-self-tests.mjs:5154-5161`; transform dry-run precedent at `:5121-5127` | source |

## Acceptance criteria

| ID | EARS criterion | Proof |
|---|---|---|
| AC-01 | The shipped rule `packages/spec/src/claude/rules/review-audit-self-decision.md` shall state every CP-01 clause, reference `hapo:ask` for ask-back conditions instead of restating them, reference the C1/C2/C3 gates instead of redefining them, and shall not contain a negation pattern (audit findings overriding a prior decision, or proceeding without waiting for the user). | `node packages/spec/scripts/run-skill-self-tests.mjs --static-only` |
| AC-02 | The shipped rule `packages/spec/src/claude/rules/process-management.md` shall state the orphan failure mode and every CP-02 clause, naming wave worktrees in the stop-before-release rule, and shall not contain a negation pattern (taking another port instead of stopping the stale owner, or treating orphaned processes as acceptable). | `node packages/spec/scripts/run-skill-self-tests.mjs --static-only` |
| AC-03 | When either rule is transformed for Codex, `normalizeCodexBody(content, sourcePath)` shall differ from the source only by `hapo:` skill-name renames, proven by a probe comparing the transform output with a rename-only projection of the same bytes; the static run shall exit 0 and report exactly three more executed tests than the pre-change baseline measured in the same session. | `node packages/spec/scripts/run-skill-self-tests.mjs --static-only` |
| AC-04 | When CafeKit installs both platforms into a temporary project, `.claude/rules/<file>` shall be byte-equal to source and `.codex/rules/<file>` shall equal both `normalizeCodexBody(source, sourcePath)` and the rename-only projection, for both new files; `packages/spec/src/codex/rules/` shall contain no file with either name; and the installed root `AGENTS.md` shall contain a Codex-native consult bullet naming both rules while keeping the runtime-vocabulary assertions of `codex-native.test.js:2200-2214` green. | `npm --prefix packages/spec test` |
| AC-05 | The packed tarball inventory shall include `src/claude/rules/review-audit-self-decision.md` and `src/claude/rules/process-management.md`. | `npm --prefix packages/spec test` |

## Tasks

| # | Task | Criteria | Primary ownership | Dependencies | Status |
|---|---|---|---|---|---|
| 01 | Author the two ported rules with static guards | AC-01–AC-03 | two new rule files + harness probes | - | done |
| 02 | Wire the Codex pointer and prove installed parity | AC-04–AC-05 | `src/codex/AGENTS.md`, `codex-native.test.js`, `package-inventory.test.js` | task-01-author-ported-rules.md | done |

Tasks are sequential: Task 02 asserts placement of the exact bytes authored by Task 01.

## Review log

- Round 1 (2026-09-02): two fresh-context reviewers (Fact Checker + Contract Verifier; Assumption Destroyer + Scope Critic + Proof lens) produced 10 deduplicated findings (2 High, 5 Medium, 3 Low). User accepted all 10 and chose the shape at three forks: add the Codex consult pointer to the runtime template (R-01), guard with ordinary probes plus forbidden patterns instead of a mutation checker (R-04), and keep two tasks with Task 01 scoped to source payload (R-05). Repairs: Codex reach claim replaced by a real pointer owned by Task 02; every anchor paired with a negation pattern; the transform-safe token list completed and a transform-safety probe moved into Task 01; overlap with `hapo:ask` and `workflow.md` resolved by reference instead of restatement; legacy ID-bearing test names excluded explicitly; citations corrected (`codex-runtime.js:111-123`, `package-inventory.test.js:110-145`); the preserved-file limitation recorded in CP-03; environment declarations corrected (`.logs` handling and the executed-count oracle). Both reviewers independently dry-ran `normalizeCodexBody` on the two reference texts: output is byte-identical to input, so every transform risk comes from added CafeKit vocabulary. C1 scope unchanged.
- Round 1 closure (2026-09-02): a fresh-context reviewer replayed all ten counterexamples against current packet bytes.

| ID | Decision | Original counterexample | Repaired at | Proved at | Replay | Closure |
|---|---|---|---|---|---|---|
| R-01 | accepted | Codex has no directory auto-load, so parity proves file presence, not instruction reach | plan CP-03, task-02 Ownership/AC-04 | template read confirms only the two routing rules are pointed at (`src/codex/AGENTS.md:9-15`); linter and template probes accept an added bullet | PASS | closed |
| R-02 | accepted | An additive mutation keeps every anchor and still reverses the rule | plan AC-01/AC-02, task-01 Acceptance | the four negation strings are absent from both reference texts, so a phrase-exact forbidden pattern cannot false-positive | PASS | closed |
| R-03 | accepted | CafeKit vocabulary silently diverges under the Codex transform | task-01 transform-safe list, transform probe moved into task 01 | per-token dry run of `normalizeCodexBody`; three further firing tokens added after replay | PASS | closed |
| R-04 | accepted | A mutation checker is disproportionate for two prose rules | task-01 Scope/Out | exactly four ordinary rule probes exist today; the cited probe shape and transform precedent are real | PASS | closed |
| R-05 | accepted | Task 01 claimed installed truth from a source-level command | task-01 Outcome | Outcome and reachability now stop at `src/claude/rules/` | PASS | closed |
| R-06 | accepted | The kit would ship an ID rule its own test names break | plan Out of scope, CP-01 "do not add" | phrasing verified; the unreproducible count was removed after replay measured 12–45 depending on pattern | PASS | closed |
| R-07 | accepted | Restating ask-back conditions and gate definitions creates a second home | plan AC-01, task-01 Scope | both referenced homes exist (`skills/question/SKILL.md:97-104`, `rules/workflow.md:15-19,62-63`) | PASS | closed |
| R-08 | accepted | Wrong line citations | plan and both tasks | all citations re-verified; seven mismatches found and corrected after replay | PASS | closed |
| R-09 | accepted | Proof covers fresh installs only | plan CP-03 limitation | `bin/lib/managed-writer.js:95-109` preserves user-created files unless forced | PASS | closed |
| R-10 | accepted | Wrong environment declarations and a trivial oracle | both tasks Artifacts/Oracle | static-only measured at 528 executed, exit 0, no `.logs` written, `git status` unchanged; `provenance.cjs:157-166` confirms source-tree `.logs` is not excluded | PASS | closed |

Sweep: 3 files reread / 38 citations plus 6 structural checks / 12 stale references fixed / 0 conflicts left. The full-suite wall clock above ten minutes remains unreplayed and is declared as an environment expectation, not a proof.
