# Grok CLI runs the CafeKit gates through the Claude install
Specs-Contract: process-first-ready-v1

## Scope decision (C1 — 2026-09-08)
- Existing: grok 1.0.13 loads `<project>/.claude/settings.json` hooks, `.claude/skills`, `.claude/rules`, and `CLAUDE*.md` on its own through `[compat.claude]`, every cell default `true`; `grok inspect` in this repository lists CafeKit's hook groups as `project [claude]`, matcher `Read|Write|Edit|MultiEdit|Bash|Glob|Grep` included. It sets `CLAUDE_PROJECT_DIR` for every hook as an alias of `GROK_WORKSPACE_ROOT`, aliases Claude tool names in matchers, and accepts the Claude output vocabulary unchanged. The hooks are already portable (`src/claude/hooks/lib/runtime-dir.cjs`). `src/omp/hooks/lib/omp-tool-names.cjs` is a tool-name alias table of the same kind grok needs.
- Minimum change: one shared payload reader that accepts grok's camelCase envelope and returns the snake_case Claude shape the hooks read; routed into thirteen of the fourteen hooks that parse stdin, the Stop gate needing only a second spelling at its loop guard; the two exit-2 denials emit a JSON deny carrying the full reason; omp's lowercase aliases move into that one table so the omp overlay shrinks to `privacy-block.cjs`; `--platform grok` installs the Claude runtime and prints the trust reminder; documentation records what grok can and cannot carry. No `.grok/` directory.
- Expansion signals: about twenty-five touched files, above the eight-file threshold; one subsystem, since every hook edit is the same one-line routing and every other file follows from it.
- User decision: C1 KEEP — normalize at the source. EXPAND (`.grok/hooks/<kit>.json` plus an envelope shim, the AgentKit approach) was declined: with `[compat.claude] hooks` on by default it runs every hook twice, once with the wrong payload. CUT was declined.

## What grok can and cannot carry (C2 finding 2, 2026-09-08)
Grok has exactly four control-flow channels: a `PreToolUse` deny, a `UserPromptSubmit` block, a `Stop`/`SubagentStop` block, and exit 2. Everything else is passive — "every other event is passive: its output is recorded but does not change control flow" (`32360`, `32425`) — and for an allowing `UserPromptSubmit` hook "an allowing hook's stdout / `additionalContext` is discarded rather than added as context" (`32587`).

So under grok this packet repairs the gates and cannot repair the reminders. These hooks keep running and keep being normalized, but the model never reads their output: `rules.cjs`, `spec-state.cjs`, `session.cjs`, `docs-sync.cjs`, `state.cjs`, and the allowing path of `secret-output-guardrail.cjs`. They are normalized anyway because it costs one line each and keeps one code path for every host; the packet claims no reminder delivery under grok, and no acceptance criterion uses their stdout as proof.

## Why this is a repair, not a feature
Today any project installed with `--platform claude` and opened in grok runs CafeKit's hooks with a payload they cannot read.

- `privacy-block.cjs:205` reads `data.tool_name`, gets `undefined` under grok's `toolName`, extracts no paths, and exits without an `ask`: **the privacy gate is silently open**.
- `spec-gate.cjs:83` reads `payload.stop_hook_active`, gets `undefined`, and loses its loop guard; grok's own cap ends the turn "after **8 continuations**" (`32508`).
- `inspect-block.cjs:92` and `task-scaffold-guard.cjs:75` print their deny reason with `console.log` and exit 2. Grok takes "the first stderr line (capped) … as the deny reason when the JSON carries none" (`32496`), and there is no stderr, so the model is blocked with no reason at all.
- `completion-authority.cjs` is registered without an argv mode on both `UserPromptSubmit` and `Stop`; with `hook_event_name` undefined its mode falls back to `--stop` (`completion-authority.cjs:140`), so under grok it runs the stop path on **every user prompt** and can emit `{"decision":"block"}`, which grok honours as a prompt block (`32362`).

## Verified contract (grok 1.0.13)
Regenerate the evidence file with `strings -n 6 ~/.grok/downloads/grok-1.0.13-macos-aarch64 > grok-strings.txt`; bare numbers below are lines in that file, each quoted by its anchor phrase so a shifted dump stays checkable.

- Sources: `<project>/.grok/hooks/*.json` and `<project>/.claude/settings.json` plus `settings.local.json`, both requiring project trust via `/hooks-trust` or `--trust`; untrusted project hooks are silently skipped (`32330`). `[compat.claude]` cells `skills`, `rules`, `agents`, `mcps`, `hooks`, `sessions` all default `true` (`31096`).
- Events: `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `PermissionDenied`, `Stop`, `StopFailure`, `StopCancelled`, `Notification`, `SubagentStart`, `SubagentStop`, `PreCompact`, `PostCompact`, `SessionEnd`; unrecognized names are skipped so a Claude settings file still loads.
- Control flow: only the four channels above (`32360`, `32425`).
- Stdin envelope, camelCase: `hookEventName` (snake value such as `pre_tool_use`), `sessionId`, `cwd`, `workspaceRoot`, `timestamp`, `permissionMode`, `promptId` (`32456`); tool events add `toolName`, `toolInput`, `toolUseId`, `toolInputTruncated`; `Stop` adds `stopHookActive`, `lastAssistantMessage`, `reason`; `PostToolUse` carries `toolResult`, not `tool_response`; subagent identity is `subagentType` (`32578`). The binary's own camel/snake pair table includes `transcriptPath`/`transcript_path` (`28215`).
- Tool names: the documented matcher aliases are `Bash`→`run_terminal_command`, `Read`→`read_file`, `Edit`/`Write`/`MultiEdit`→`search_replace`, `Grep`→`grep`, `Glob`/`ListDir`→`list_dir`, `WebSearch`→`web_search`, `Task`→`spawn_subagent`, and "a matcher keeps its original name too" (`32405`-`32419`). The binary's internal tool-name pool additionally contains `write`, `delete_file`, and `PowerShell` (`28227`), so grok's file-creating tool is `write`, distinct from `search_replace`. `read_file` input uses `path` (`33496`).
- Output: `PreToolUse` reads top-level `decision` or `hookSpecificOutput.permissionDecision`, the latter canonical when present (`32473`); **"a `deny` decision in stdout JSON is honored regardless of exit code"** (`32498`). `Stop` blocks on `{"decision":"block","reason"}` (`32502`) or exit 2 with stderr as feedback. `UserPromptSubmit` blocks on exit 2 or `{"decision":"block"}` (`32362`).
- Failure mode: every hook failure — timeout, crash, malformed output, any exit code other than 0 or 2 — is fail-open (`32402`, `32425`). Default timeout 5 s, 600 s for `Stop`/`SubagentStop` (`32402`).
- Environment: `GROK_HOOK_EVENT`, `GROK_HOOK_NAME`, `GROK_SESSION_ID`, `GROK_WORKSPACE_ROOT`, `CLAUDE_PROJECT_DIR` on every hook (`32615`); `${VAR}` in `command` is expanded once by the runner (`32451`).
- Matchers are regular expressions and are tested against the tool name on tool events, the start source on `SessionStart`, the subagent type on `SubagentStart`/`SubagentStop`; a matcher on `Stop` or `UserPromptSubmit` is ignored with a warning (`32398`).
- AgentKit (`ak`) registers `.grok/hooks/<kit>.json` commands as `node ${GROK_WORKSPACE_ROOT}/.grok/hooks/<kit>/agentkit-grok-envelope-shim.cjs <hook>` and translates camelCase→snake_case, `run_terminal_command`→`Bash`, `pre_tool_use`→`PreToolUse`, exit 2→`{"decision":"deny"}` in a ~211-line fail-open shim [from the 2026-09-08 research session; the installed copy was in a discarded temp project].

### `[UNVERIFIED]` — C2 decision: recorded as limitations, not settled by a live run
The user declined spending a grok turn to settle these. Each names the command that would settle it; no acceptance criterion depends on any of them.
- **Matcher `"*"`**: `src/claude/settings/settings.json` uses it for `SubagentStart` and `PreCompact`, but grok documents matchers as regular expressions (`32398`) and its own hooks use `.*`. `grok inspect` shows the groups loading, which is not the same as matching. If they never match, `agent.cjs` and `precompact.cjs` never run under grok. Settle: register a `SubagentStart` hook with matcher `*` writing to a file in a throwaway repository and run one grok turn with a subagent.
- **`grep` / `list_dir` input schema**: `inspect-block.cjs:91` gates on `toolInput.pattern`; grok's key for that argument is not documented in the dump. Settle: the same envelope dump on a real `grep` call.
- **Field names for `prompt`, `source`, `trigger`, `agent_id`**: the common-field list (`32456`) omits them and the dump does not name grok's spellings. The reader accepts both the Claude spelling and the obvious camelCase twin, so a match works either way, but neither is proven. Settle: the same envelope dump on `UserPromptSubmit` and `SessionStart`.

## Out of scope
- A `.grok/` runtime directory, `.grok/hooks/*.json` registration, or an envelope shim. Users who set `[compat.claude] hooks = false` are not served; documented.
- Reminder delivery under grok, as recorded above. The six context-injecting hooks are normalized but prove nothing.
- **`usage.cjs` (C2 finding 8)**: it reads the Claude Code OAuth token from the macOS Keychain (`usage.cjs:54`) and calls `api.anthropic.com` (`usage.cjs:122`), and its `isUserPrompt` flag lowers the fetch interval from 300 s to 60 s (`usage.cjs:43-45,170`). Normalizing its payload would make a non-Claude runtime touch Claude credentials five times as often for output grok discards. It is left unrouted and keeps behaving exactly as it does today.
- `state.cjs` transcript parsing under grok: `transcriptPath` is deliberately **not** mapped, so `state.cjs:115` keeps skipping silently rather than streaming a grok session file of unknown format. Recorded as a limitation.
- `status.cjs`: it does parse stdin (`status.cjs:91-94,459`), but grok's `[compat.claude]` has no statusline cell (`31096`), so grok never invokes it.
- Launching grok in tests. Every proof runs the real hooks as child processes with envelopes built from the documented shape.
- README platform lists, matching the omp packet's exclusion.

## Coverage profile
| ID | Outcome | Change kinds | Material surfaces | Ambiguity/action | Risk/evidence | Required proof |
|---|---|---|---|---|---|---|
| CP-01 | A grok or omp envelope becomes the Claude payload the hooks read; a Claude payload keeps its shape | add | `src/claude/hooks/lib/hook-payload.cjs` | settled — tool-name aliasing applies to the value under any key; the reader never throws | critical — the privacy gate depends on the tool name and paths being seen | source |
| CP-02 | Every gate hook reads through the reader, and a denial carries its full reason to the model | modify 14 hooks, manifest, 5 fixtures | `src/claude/hooks/*.cjs`, `src/claude/migration-manifest.json`, five test allow-lists | settled | critical — shipped Claude platform; a reader exception above a fail-closed loop guard would block Stop | source + installed |
| CP-03 | omp keeps its contract with a one-file overlay | remove, modify | `src/omp/hooks/`, three omp tests, overlay sentences in docs and changelogs | settled by C1 | elevated — shipped omp platform | source + installed |
| CP-04 | `--platform grok` provisions the Claude runtime and tells the user to trust the project | add | `bin/lib/context.js`, `bin/phases/select-platform.js`, `bin/lib/i18n.js` | settled — alias only, no detection hint | elevated — installer surface | installed |
| CP-05 | Documentation records grok compatibility and its limits | modify | `docs/installer-architecture.md`, changelogs, static probe | settled | routine | source |

## Acceptance criteria
| ID | EARS criterion | Proof |
|---|---|---|
| AC-01 | When `normalizeHookPayload` receives a grok envelope (`hookEventName: "pre_tool_use"`, `sessionId`, `toolName`, `toolInput`), it shall return `hook_event_name: "PreToolUse"`, `session_id`, `tool_name`, `tool_input`; `read_file` with `toolInput.path` shall yield `tool_name: "Read"` and `tool_input.file_path`; `stopHookActive`, `lastAssistantMessage`, `toolResult`, `subagentType`, `subagentId` shall map to `stop_hook_active`, `last_assistant_message`, `tool_response`, `agent_type`, `agent_id`; every name reaching a `Bash` matcher (`run_terminal_command`, `run_terminal_cmd`, `PowerShell`, `bash`) shall map to `Bash`, and `write` shall map to `Write` while `search_replace` maps to `Edit`; a Claude-shaped payload shall keep every key spelling and value it had, except that `tool_name` is always passed through the alias table so omp's lowercase names reach the Claude names; an unknown tool name shall pass through unchanged; a hostile input (array, null, non-string `toolName`, non-object `toolInput`) shall return without throwing. | `node --test bin/__tests__/hook-payload.test.js` |
| AC-02 | When the real hooks run from a copied `.claude` tree with grok-shaped stdin, `privacy-block.cjs` shall answer `permissionDecision: "ask"` for `run_terminal_command` reading `.env` and for `read_file` with `path` `.env`; `spec-gate.cjs` shall exit 0 with empty stdout when `stopHookActive` is true and shall still block when it is absent and a done task lacks proof; `completion-authority.cjs` shall take its approve path on `user_prompt_submit` and emit no `decision: "block"`; `inspect-block.cjs` and `task-scaffold-guard.cjs` shall emit `hookSpecificOutput.permissionDecision: "deny"` on stdout carrying the **full** reason text and shall still exit 2; `secret-output-guardrail.cjs` shall reach its match path for a credential-shaped prompt. | `node --test bin/__tests__/grok-envelope.test.js` |
| AC-03 | When the hook suite, the two installer suites that copy hooks, the omp overlay suite, and the packed-install cases run after the routing, they shall pass at their prior counts plus the new cases. | `node --test src/claude/hooks/__tests__/*.test.js && node --test bin/__tests__/develop-contract.test.js bin/__tests__/specs-v2-execution-closeout.test.js bin/__tests__/omp-hooks.test.js && node --test --test-name-pattern "packed Claude and Codex installs" bin/__tests__/package-inventory.test.js` |
| AC-04 | When `src/omp/hooks/` is compared to `src/claude/hooks/`, it shall contain only `privacy-block.cjs`, equal to the Claude file plus its ask→deny edits; an omp install shall still deny a lowercase `bash` read of `.env` and still guard a lowercase `write` into a nested task path, through the shared alias table; `.omp/hooks/lib/omp-tool-names.cjs` shall not install. | `node --test bin/__tests__/omp-hooks.test.js bin/__tests__/omp-runtime.test.js bin/__tests__/omp-bridge.test.js` |
| AC-05 | When the installer runs with `--platform grok`, it shall install the Claude runtime under `.claude/`, create no `.grok/`, print the trust reminder naming `grok --trust` and `/hooks-trust` once in the selected language, and record `platform: claude`; `--platform grok,claude` shall install once; `--platform claude` shall print no grok notice; a project holding only `.grok/` shall not be auto-detected as a Claude install. | `node --test bin/__tests__/grok-platform.test.js` |
| AC-06 | When documentation describes grok, it shall state that grok reads the Claude install through `[compat.claude]`, that `lib/hook-payload.cjs` normalizes grok's camelCase envelope inside the hooks, that only four control-flow channels exist so the reminder hooks do not reach the model, that `usage.cjs` is deliberately unrouted, that project hooks run only after trust, and that `[compat.claude] hooks = false` is unsupported; the omp overlay shall be described as one file; both changelogs shall gain a `Fixed` entry. | `node scripts/run-skill-self-tests.mjs` |

## Tasks
| # | Task | Criteria | Primary ownership | Dependencies | Status |
|---|---|---|---|---|---|
| 01 | One reader turns any supported envelope into the Claude payload | AC-01 | `src/claude/hooks/lib/hook-payload.cjs`, `bin/__tests__/hook-payload.test.js` | - | done |
| 02 | Every gate hook reads through it and denies with a visible reason | AC-02, AC-03 | 14 hook files (13 routed, the Stop gate guard-only), `src/claude/migration-manifest.json`, 5 fixture allow-lists, `bin/__tests__/grok-envelope.test.js`, the overlay anchor in `bin/__tests__/omp-hooks.test.js` | task-01-hook-payload-reader.md | done |
| 03 | The omp overlay shrinks to the privacy hook | AC-04 | `src/omp/hooks/`, three omp tests, the overlay sentences in `docs/installer-architecture.md`, both changelogs, and their probe | task-02-route-hooks-through-reader.md | done |
| 04 | `--platform grok` installs the Claude runtime and asks for trust | AC-05 | `bin/lib/context.js`, `bin/phases/select-platform.js`, `bin/lib/i18n.js`, `bin/__tests__/grok-platform.test.js` | task-02-route-hooks-through-reader.md | pending |
| 05 | Documentation records grok compatibility and its limits | AC-06 | `docs/installer-architecture.md`, `packages/spec/CHANGELOG.md`, `docs/project-changelog.md`, `packages/spec/scripts/run-skill-self-tests.mjs` | task-03-fold-omp-aliases.md, task-04-grok-platform-alias.md | pending |

## Review log
- Round 1 (2026-09-08): three fresh-context reviewers (fact-checker plus security adversary; flow tracer plus failure-mode analyst; contract verifier plus scope critic), all three CONCERNS. 36 raw findings deduplicated by root cause to 15; all 15 accepted, four of them as user decisions.
  - **User decisions.** Finding 2 (only four control-flow channels; six reminder hooks silent): KEEP the packet and record the limitation, which added the "What grok can and cannot carry" section and removed `rules.cjs` stdout from AC-02. Finding 8 (`usage.cjs` reads Keychain credentials and calls the network; normalizing raises that rate fivefold): remove it from routing, dropping task 02 from fifteen hooks to fourteen. Finding 12 (grok reads only the first stderr line, so the scaffold command would still be lost): emit a JSON deny on stdout carrying the full reason, which grok honours "regardless of exit code" (`32498`), instead of mirroring to stderr. Three `[UNVERIFIED]` items: record as limitations rather than spend a live grok turn.
  - **Objective defects repaired.** AC-02 demanded a `search_replace`-named Write that the alias table made impossible, and grok's file-creating tool is `write` (`28227`), not `search_replace`. AC-01 demanded both a deep-equal pass-through and omp lowercase aliasing, which are mutually exclusive for omp's snake_case-key/lowercase-value payload; the rule is now that the alias applies to the value under any key. Inserting the reader above the loop guard of the two fail-closed hooks made a reader exception block Stop through `spec-gate.cjs`'s final catch, so the guard now reads the raw payload first and the reader must be total. The key map gained `prompt`, `source`, `trigger`, and `agent_id`/`subagentId`; the alias table gained `PowerShell`, `run_terminal_cmd`, `grep_search`, `write`, and `delete_file`, because any name reaching a `Bash` matcher that is not mapped kills the command scan silently. `completion-authority.cjs`'s missing argv mode was added to the defect list. Task 02 now runs `omp-hooks.test.js`, whose byte-for-byte overlay comparison its own edit breaks, and owns that anchor. Task 04 lost the `.grok` detection hint (it would auto-install Claude into a grok-only repository), gained the alias resolution point before language selection with a second deduplication, and names `select-platform.js` as the one file that prints the notice. Task 03 gained the three `[Unreleased]` changelog sentences and the whole `.omp/hooks/` bullet. `transcriptPath` was removed from the key map so the plan and the task stop contradicting each other. The `status.cjs` exclusion reason was corrected: it does read stdin, but grok has no statusline compatibility cell. `semantic-review-authority.cjs:147` parses at its call site and is routed there explicitly. Five citation line numbers were re-derived and each contract line now carries its anchor phrase.
  - **Revised rather than applied as written.** The reviewers proposed splitting task 02. It stays one task: the edit is the same mechanical line in each hook, the previous packet closed an equivalent fourteen-hook routing task in one pass, and one verification command covering four suites is the guard.
  - Sweep: 6 files reread / 15 deltas / 9 stale references fixed / 0 conflicts left.
